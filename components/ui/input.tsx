import { cva } from "class-variance-authority";

export const inputStyles = cva([
    "border-2 border-border bg-background text-foreground p-3 rounded-xl focus-visible:outline-none text-sm font-mono",
]);
